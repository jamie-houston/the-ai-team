Help with SQL queries or database operations.

Use the SQL agent defined in `.claude/agents/specialized/dotnet/sql.md` to:
1. Write complex LINQ queries
2. Generate raw SQL when needed
3. Optimize query performance
4. Add indexes
5. Debug EF Core generated SQL

Question or task: $ARGUMENTS
