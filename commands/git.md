---
description: Git workflow help - status, commits, history
argument-hint: [action: status|commit|log|undo]
---

Help with git operations and commit workflow.

Use the Git agent defined in `.claude/agents/universal/git.md` to:
1. Check status and recent history
2. Create well-formatted commit messages
3. Stage and commit changes
4. Handle git issues

Action: $ARGUMENTS

Common actions: status, commit, log, undo
