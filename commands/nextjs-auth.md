---
description: Set up NextAuth.js authentication with OAuth and credentials
argument-hint: [providers]
---

# Set Up Authentication

Use the Next.js Auth agent to configure NextAuth.js with OAuth providers (GitHub, Google) and/or credentials authentication.

Providers: $ARGUMENTS

If no providers specified, set up GitHub OAuth and credentials auth.
